// business logic
var pingPong = function(digit) {
  if ((entry % 3 === 0 ) && (entry % 5 === 0 ) && (entry % 15 === 0) || (entry !== num)) {
    return true;
  } else {
    return false;

var pingPong = (15,30,45,60);
    var ping = (3,6,9,12,15,18,21,24,27,30,33,36,39,42,45,48,51,54,57,60);
    var pong = (5,10,15,20,25,30,35,40,45,50,55,60);
    // var result = (entry);
    var digit = parseInt($("input#digit").val());

      // if ((ping).show("Ping!")||(pong).show("Pong"(pingPong)||.show("PingPong!!")){


    if (fasle);
      return ("Please enter a positive number between 1 and 60")
      console.log(result)
    }
};

// user interface logic
$(document).ready(function() {
  $("form#pingPong").submit(function(event) {
    event.preventDefault();
    // var pingPong = (15,30,45,60);
    // var ping = (3,6,9,12,15,18,21,24,27,30,33,36,39,42,45,48,51,54,57,60);
    // var pong = (5,10,15,20,25,30,35,40,45,50,55,60);
    // var result = ("pingPong"(entry));
    var digit = parseInt($("input#digit").val());
//     var year = parseInt($("input#year").val());
    // var result = pingPong (digit);

    $(".digit").text(digit);

    if (!result) {                 // same as writing if (result === false)
      return ("please enter a positive number between 1-60");
    }else if
      return ( ping.show("Ping!")|| pong .show("Pong" pingPong ||.show("PingPong!!");
    }
//
    $("#result").show("#result");
    console.log()
  });
});
